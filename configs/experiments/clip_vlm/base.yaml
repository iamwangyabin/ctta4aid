extends:
  - ../../protocols/clip_vlm_single_target.yaml
  - ../../methods/source_ft.yaml
  - ../../methods/tent.yaml
  - ../../methods/eata.yaml
  - ../../methods/sar.yaml
  - ../../methods/cotta.yaml
  - ../../methods/lame.yaml
  - ../../methods/t2a.yaml
  - ../../methods/frozen_clip.yaml
  - ../../methods/tda.yaml
  - ../../methods/dynaprompt.yaml
  - ../../methods/cliptta.yaml
  - ../../methods/batclip.yaml
  - ../../methods/iapl.yaml
  - ../../methods/ours.yaml

device: auto
methods:
  - source_ft
  - tent
  - eata
  - sar
  - cotta
  - lame
  - t2a
  - frozen_clip
  - tda
  - dynaprompt
  - cliptta
  - batclip
  - iapl
  - ours_static
  - ours

model:
  family: clip_vlm_main
  source_detector:
    architecture: ViT-L/14
    checkpoint: ${CLIP_VIT_L14_CHECKPOINT}
    source_checkpoint: ${CLIP_VITL14_SOURCE_CHECKPOINT}
    image_size: 224
    resize_size: 256
    trainable_scope: full
  clip_native:
    architecture: ViT-L/14
    checkpoint: ${CLIP_VIT_L14_CHECKPOINT}
    image_size: 224
    resize_size: 256

data:
  image_size: 224
  batch_size: 16
  # Both 4090 hosts have reproduced worker-process segmentation faults during
  # long CLIP runs. This changes only input delivery; manifests keep sample
  # identities and order fixed for every method and seed.
  num_workers: 0
  shuffle: false
  max_samples_per_class: null

method_configs:
  source_ft:
    capture_initial_state: false
    data:
      num_workers: 0
  tent:
    capture_initial_state: false
    clip_visual_layernorm: true
    data:
      batch_size: 16
      num_workers: 0
  eata:
    capture_initial_state: false
    clip_visual_layernorm: true
    data:
      batch_size: 16
      num_workers: 0
  sar:
    capture_initial_state: false
    data:
      batch_size: 16
      num_workers: 0
  cotta:
    capture_initial_state: false
    data:
      batch_size: 16
      num_workers: 0
  lame:
    capture_initial_state: false
    data:
      batch_size: 16
      num_workers: 0
  t2a:
    capture_initial_state: false
    clip_visual_layernorm: true
    data:
      batch_size: 16
      num_workers: 0
  frozen_clip:
    capture_initial_state: false
    data:
      batch_size: 16
      num_workers: 0
  tda:
    capture_initial_state: false
    data:
      batch_size: 16
      num_workers: 0
  dynaprompt:
    capture_initial_state: false
    data:
      batch_size: 1
      num_workers: 0
  cliptta:
    capture_initial_state: false
    data:
      batch_size: 16
      num_workers: 0
  batclip:
    capture_initial_state: false
    data:
      batch_size: 16
      num_workers: 0
  iapl:
    checkpoint: ${IAPL_GENIMAGE_CHECKPOINT}
    adaptation_mode: full
    normalization_mean: [0.481, 0.458, 0.408]
    normalization_std: [0.269, 0.261, 0.276]
    smooth: true
    data:
      batch_size: 1
      num_workers: 0
  ours_static:
    data:
      batch_size: 16
      num_workers: 0
  ours:
    data:
      batch_size: 16
      num_workers: 0

reporting:
  primary_table: clip_vitl14_method_native_online
  source_setup_column: required
  excluded_from_main_quantitative_table: [rotta, ttc]
  paired_static_baselines:
    ours: ours_static
