extends: matched_jpeg_genimage_seed3.yaml

methods:
  - ours_static
  - ours

method_configs:
  ours:
    feature_replay_seed: 3

output_dir: ${CTTA4AID_EXPERIMENT_ROOT}/clip_vlm_bias_controlled/matched_jpeg/ours_single_target/genimage/seed3

reporting:
  formal_method: ours
  internal_version: R47
  selection_seed: 1
  validation_seeds: [0, 2, 3]
  paired_static_baselines:
    ours: ours_static
