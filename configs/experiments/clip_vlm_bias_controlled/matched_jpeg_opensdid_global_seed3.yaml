extends:
  - ../clip_vlm/opensdid_global_seed3.yaml
  - matched_jpeg.yaml

output_dir: ${CTTA4AID_EXPERIMENT_ROOT}/clip_vlm_bias_controlled/matched_jpeg/opensdid_global/seed3

data:
  root: ${OPENSDID_GLOBAL_MATCHED_JPEG_ARROW_ROOT}
