extends:
  - ../controlled_ctta/opensdid_global_continual_seed1.yaml
  - matched_jpeg.yaml
  - ../../methods/ascal_gmm_segmented_memory_posterior_clip_routed_gaussian_replay_mlp.yaml

methods:
  - ascal_gmm_segmented_memory_posterior_clip_routed_gaussian_replay_mlp_static
  - ascal_gmm_segmented_memory_posterior_clip_routed_gaussian_replay_mlp

method_configs:
  ascal_gmm_segmented_memory_posterior_clip_routed_gaussian_replay_mlp:
    feature_replay_seed: 1

output_dir: ${CTTA4AID_EXPERIMENT_ROOT}/clip_vlm_bias_controlled/matched_jpeg/ascal_gmm_segmented_memory_posterior_clip_routed_gaussian_replay_mlp_continual/opensdid_global/seed1

data:
  root: ${OPENSDID_GLOBAL_MATCHED_JPEG_ARROW_ROOT}
  num_workers: 0
  shuffle: false
