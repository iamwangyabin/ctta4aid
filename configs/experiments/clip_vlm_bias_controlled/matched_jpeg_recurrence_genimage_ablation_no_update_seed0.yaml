extends: matched_jpeg_recurrence_genimage_seed0.yaml

methods:
  - ours

method_configs:
  ours:
    ablation_mode: no_update

output_dir: ${CTTA4AID_EXPERIMENT_ROOT}/clip_vlm_bias_controlled/matched_jpeg/genimage_recurrence/seed0/ablations/no_update

reporting:
  study: no_boundary_component_ablation
  disabled_component: update
  reference_method: ours
