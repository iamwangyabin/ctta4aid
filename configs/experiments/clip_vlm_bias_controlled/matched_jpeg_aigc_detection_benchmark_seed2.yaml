extends:
  - ../clip_vlm/aigc_detection_benchmark_seed2.yaml
  - matched_jpeg.yaml

output_dir: ${CTTA4AID_EXPERIMENT_ROOT}/clip_vlm_bias_controlled/matched_jpeg/aigc_detection_benchmark/seed2

data:
  root: ${AIGC_DETECTION_BENCHMARK_MATCHED_JPEG_ARROW_ROOT}
