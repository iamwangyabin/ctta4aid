# Run all CNN methods directly on the original UFD Arrow bundles.
extends: universalfake_single_target.yaml

data:
  format: hf_arrow
  root:
    - ${UFD_FORENSYNTHS_ARROW_ROOT}
    - ${UFD_OJHA_ARROW_ROOT}
  split: test

output_dir: outputs/universalfake_arrow/single_target/all_methods
