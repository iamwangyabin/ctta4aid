extends: caidbench_sd14_resnet50.yaml
output_dir: outputs/smoke/source_train/caidbench_sd14_resnet50

data:
  batch_size: 16
  num_workers: 2
  max_train_samples_per_class: 64
  max_val_samples_per_class: 32

training:
  epochs: 1
  fisher_samples: 32
  fisher_batch_size: 16
