# Run the fixed UFD stream directly on the original Arrow bundles.
extends: universalfake_continual_stream.yaml

data:
  format: hf_arrow
  root:
    - ${UFD_FORENSYNTHS_ARROW_ROOT}
    - ${UFD_OJHA_ARROW_ROOT}
  split: test

output_dir: outputs/universalfake_arrow/continual/all_methods
