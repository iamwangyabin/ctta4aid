diff --git a/models/clip_models.py b/models/clip_models.py
--- a/models/clip_models.py
+++ b/models/clip_models.py
@@ -116 +116 @@ class CLIPModel(nn.Module):
-        clip_model = load_clip_to_cpu('/Path/to/ViT-L-14.pt', cfg['N_CTX'], args.vit_adapter_list, args.text_adapter_list, args.prompt_depth, args.gate)
+        clip_model = load_clip_to_cpu(args.clip_path, cfg['N_CTX'], args.vit_adapter_list, args.text_adapter_list, args.prompt_depth, args.gate)
diff --git a/main.py b/main.py
--- a/main.py
+++ b/main.py
@@ -155 +155 @@ def main(args):
-        checkpoint = torch.load(args.pretrained_model, map_location='cpu')
+        checkpoint = torch.load(args.pretrained_model, map_location='cpu', weights_only=False)
@@ -183 +183 @@ def main(args):
-        checkpoint = torch.load(args.pretrained_model, map_location='cpu')
+        checkpoint = torch.load(args.pretrained_model, map_location='cpu', weights_only=False)
diff --git a/test_time.py b/test_time.py
--- a/test_time.py
+++ b/test_time.py
@@ -61 +61 @@ def testtime_main(args):
-    checkpoint = torch.load(args.pretrained_model, map_location='cpu')
+    checkpoint = torch.load(args.pretrained_model, map_location='cpu', weights_only=False)
@@ -64 +64 @@ def testtime_main(args):
-    pretrained_ctx = torch.load(args.pretrained_model, map_location='cpu')['model']['prompt_learner.ctx']
+    pretrained_ctx = checkpoint['model']['prompt_learner.ctx']
diff --git a/utils/dataset.py b/utils/dataset.py
--- a/utils/dataset.py
+++ b/utils/dataset.py
@@ -4,0 +5 @@ from torch.utils.data import ConcatDataset
+from online_aig_tta.data.hf_arrow import build_iapl_arrow_dataset
@@ -75,0 +77,9 @@ class Dataset_Creator:
+            arrow_dataset = build_iapl_arrow_dataset(
+                self.dataset_path,
+                split=spilt_dataset,
+                subset=subset,
+                transform=self.transforms[spilt_dataset],
+            )
+            if arrow_dataset is not None:
+                sub_datasets.append(arrow_dataset)
+                continue
